"""Marketing automation — agentic content posting, A/B testing, and optimization.

Modules:
    anonymity_guard     — Identity filter (blocks founder name/bio, allows first-person voice)
    calendar_parser     — Parse content-calendar.md into structured actions
    content_bank        — Parse social-media.md, newsletters.md into content pieces
    content_optimizer   — LLM variant generation + performance learning loop
    analytics           — Plausible server-side API + UTM tracking
    social_twitter      — Twitter/X API v2 posting + metrics
    social_reddit       — Reddit API posting + approval queue
    social_newsletter   — Newsletter via Resend
"""
